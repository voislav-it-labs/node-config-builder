{
  "name": "@node-config-builder/core",
  "version": "0.0.4-alpha.0",
  "main": "dist/public_api",
  "types": "dist/public_api",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rimraf dist && rimraf tsconfig.build.tsbuildinfo",
    "compile": "tsc -b tsconfig.build.json"
  },
  "devDependencies": {
    "rimraf": "^3.0.0",
    "typescript": "^3.6.4"
  },
  "publishConfig": {
    "access": "public"
  }
}
